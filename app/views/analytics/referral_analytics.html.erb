<div class="container mx-auto px-4 py-8">
  <h1 class="text-4xl font-bold text-green-400 mb-8">🔗 Referral Analytics</h1>

  <!-- Referral Stats Cards -->
  <section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
    <div class="bg-gray-900 p-6 rounded-xl shadow-lg border border-green-700 text-center hover:shadow-green-500/70 transition">
      <p class="text-green-300 text-sm uppercase mb-2 tracking-wider font-semibold">Total Referrals</p>
      <p class="text-4xl font-extrabold text-green-100"><%= @total_referrals %></p>
    </div>
    
    <div class="bg-gray-900 p-6 rounded-xl shadow-lg border border-green-700 text-center hover:shadow-green-500/70 transition">
      <p class="text-green-300 text-sm uppercase mb-2 tracking-wider font-semibold">Active Referrals</p>
      <p class="text-4xl font-extrabold text-green-100"><%= @active_referrals %></p>
    </div>
    
    <div class="bg-gray-900 p-6 rounded-xl shadow-lg border border-green-700 text-center hover:shadow-green-500/70 transition">
      <p class="text-green-300 text-sm uppercase mb-2 tracking-wider font-semibold">Conversion Rate</p>
      <p class="text-4xl font-extrabold text-green-100"><%= @referral_conversion_rate %>%</p>
    </div>
    
    <div class="bg-gray-900 p-6 rounded-xl shadow-lg border border-green-700 text-center hover:shadow-green-500/70 transition">
      <p class="text-green-300 text-sm uppercase mb-2 tracking-wider font-semibold">Earnings</p>
      <p class="text-4xl font-extrabold text-green-100">$<%= number_with_precision(@referral_earnings, precision: 2) %></p>
    </div>
  </section>

  <!-- Referral Signups Over Time -->
  <section class="mb-8">
    <h2 class="text-3xl font-semibold text-green-400 mb-4">📅 Referral Signups Over Time</h2>
    <div class="bg-gray-900 p-6 rounded-xl shadow-lg border border-green-700">
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-green-800 text-sm">
          <thead class="bg-black text-green-100 uppercase tracking-wider">
            <tr>
              <th class="bg-black text-green-100 px-6 py-3 text-left">Date</th>
              <th class="bg-black text-green-100 px-6 py-3 text-left">Signups</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-green-800 text-green-300">
            <% @referral_signups_by_day.each do |date, count| %>
              <tr class="hover:bg-green-900 transition-colors">
                <td class="px-6 py-3"><%= Date.parse(date).strftime("%b %d, %Y") if date.is_a?(String) %></td>
                <td class="px-6 py-3"><%= count %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- Top Referrers -->
  <section class="mb-8">
    <h2 class="text-3xl font-semibold text-green-400 mb-4">🏆 Top Referrers</h2>
    <div class="bg-gray-900 p-6 rounded-xl shadow-lg border border-green-700">
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-green-800 text-sm">
          <thead class="bg-black text-green-100 uppercase tracking-wider">
            <tr>
              <th class="bg-black text-green-100 px-6 py-3 text-left">Rank</th>
              <th class="bg-black text-green-100 px-6 py-3 text-left">User</th>
              <th class="bg-black text-green-100 px-6 py-3 text-left">Referrals</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-green-800 text-green-300">
            <% @top_referrers.each_with_index do |(user, count), index| %>
              <tr class="hover:bg-green-900 transition-colors">
                <td class="px-6 py-3">#<%= index + 1 %></td>
                <td class="px-6 py-3"><%= user.email %></td>
                <td class="px-6 py-3"><%= count %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </section>
</div>